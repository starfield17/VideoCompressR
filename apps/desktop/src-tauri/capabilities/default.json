{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop permissions for Video Compressor",
  "windows": ["main", "queue", "activity", "presets", "settings", "preview"],
  "permissions": ["core:default", "dialog:default"]
}
