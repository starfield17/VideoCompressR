# Golden compatibility fixtures

Golden files contain normalized, path-independent contracts for planning,
FFmpeg argv, and queue transitions. Runtime-generated paths, timestamps, and
UUIDs are deliberately excluded.
