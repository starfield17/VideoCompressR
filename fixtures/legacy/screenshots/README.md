The reference GUI was inspected from source because this host has no Qt display
session.  `UX_CONTRACT.md` records the stable widget/window matrix; hosted
desktop E2E produces screenshots for the rendered Tauri application.
